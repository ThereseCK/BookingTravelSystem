package therese_get;

public class Train extends Departures{

    protected Train(int departureId, double departureTime, double travelDistance, double travelTime) {
        super(departureId, departureTime, travelDistance, travelTime);
    }
}
